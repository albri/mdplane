# =============================================================================
# mdplane Web Environment Variables
# Copy this file to apps/web/.env.local for local development.
# =============================================================================

# API origin used by web data fetching and auth client calls.
NEXT_PUBLIC_API_URL=http://127.0.0.1:3001
# Server-side internal API origin override (useful for Docker networks).
API_INTERNAL_URL=http://127.0.0.1:3001

# Public app origin used for metadata/canonical URLs.
NEXT_PUBLIC_APP_URL=http://127.0.0.1:3000

# Enable/disable OAuth-governed control plane routes in the web app.
NEXT_PUBLIC_GOVERNED_MODE=false

# WebSocket origin (include /ws when applicable).
NEXT_PUBLIC_WS_URL=ws://127.0.0.1:3001/ws

# Test-only escape hatch for production-mode E2E against local API/WS.
# Never set this in deployed environments.
# NEXT_PUBLIC_MDPLANE_ALLOW_LOCALHOST_PUBLIC_URLS=false
