import { APP_NAME, URLS } from '@mdplane/shared';

export const SITE = {
  name: APP_NAME,
  url: URLS.LANDING,
  docsUrl: URLS.DOCS,
  appUrl: URLS.APP,
  apiUrl: URLS.API,
  statusUrl: URLS.STATUS,
  githubUrl: URLS.GITHUB,
  githubActionsUrl: URLS.GITHUB_ACTIONS,
  contactEmail: URLS.CONTACT_EMAIL,
}
